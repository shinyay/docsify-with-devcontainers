services:
  playground-docsify:
    container_name: 'docsify'
    hostname: 'docsify'
    build: 
      context: .
      dockerfile: Dockerfile
    restart: always
    working_dir: '/workspace'
    tty: true
    volumes:
      - type: bind
        source: ../workspace
        target: /workspace
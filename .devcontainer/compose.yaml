services:
  playground-typescript:
    container_name: 'playground-docsify'
    hostname: 'docsify'
    build: 
      context: .
      dockerfile: Dockerfile
    restart: always
    working_dir: '/workspace'
    tty: true
    volumes:
      - type: bind
        source: ../workspace
        target: /workspace